/* eslint-disable -- auto-generated by tsc; style controlled by tsconfig */
"use strict";
/* eslint-disable -- auto-generated by tsc; style controlled by tsconfig */
/**
 * 通用状态机（数据驱动，TypeScript 源文件 - Sprint 13 迁移）
 *
 * 解决：
 *   - 散落在 orderService/orders.js#allowedTransitions、adminService/services/stateMachine.js 等处的重复 if-else
 *   - 状态转移合法性校验不统一
 *
 * 用法：
 *   const { createStateMachine, IllegalTransitionError } = require('./common/state-machine')
 *
 *   const orderSM = createStateMachine({
 *     initial: 'pending',
 *     states: ['pending', 'paid', 'shipped', 'completed', 'cancelled'],
 *     transitions: {
 *       pending: ['paid', 'cancelled'],
 *       paid: ['shipped', 'cancelled'],
 *       shipped: ['completed'],
 *       completed: [],
 *       cancelled: [],
 *     },
 *   })
 *
 *   orderSM.canTransition('pending', 'paid')      // true
 *   orderSM.assertTransition('pending', 'paid')   // OK
 *   orderSM.assertTransition('pending', 'completed') // throws IllegalTransitionError
 *   orderSM.nextStates('pending')                  // ['paid', 'cancelled']
 *   orderSM.isTerminal('cancelled')                // true
 *
 * 兼容：
 *   - adminService/services/stateMachine.js 中的 4 张表可平滑迁移到本模块
 *
 * 编译方式：
 *   npx --yes -p typescript@5.4.5 tsc -p tsconfig.common.json
 *   （运行时仍消费 .js 编译产物）
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyEvent = exports.createStateMachine = exports.validateConfig = exports.IllegalTransitionError = void 0;
const errors_1 = require("./errors");
/**
 * 非法状态转移异常
 */
class IllegalTransitionError extends Error {
    constructor(from, to, allowed, message = null) {
        super(message || `非法状态转移：${from} → ${to}（允许：${allowed.join(', ') || '无'}）`);
        this.name = 'IllegalTransitionError';
        this.from = from;
        this.to = to;
        this.allowed = allowed;
    }
}
exports.IllegalTransitionError = IllegalTransitionError;
/**
 * 校验 transitions 配置合法性
 * @param config
 * @throws {BusinessError} 配置错误
 */
function validateConfig(config) {
    if (!config || typeof config !== 'object') {
        throw (0, errors_1.err)('INVALID_PARAMS', 'state-machine config 必须为对象');
    }
    if (!config.initial) {
        throw (0, errors_1.err)('INVALID_PARAMS', 'state-machine config 必须指定 initial 状态');
    }
    if (!Array.isArray(config.states) || config.states.length === 0) {
        throw (0, errors_1.err)('INVALID_PARAMS', 'state-machine config.states 必须为非空数组');
    }
    if (typeof config.transitions !== 'object') {
        throw (0, errors_1.err)('INVALID_PARAMS', 'state-machine config.transitions 必须为对象');
    }
    const states = new Set(config.states);
    if (!states.has(config.initial)) {
        throw (0, errors_1.err)('INVALID_PARAMS', `initial 状态 "${config.initial}" 不在 states 中`);
    }
    for (const [from, targets] of Object.entries(config.transitions)) {
        if (!states.has(from)) {
            throw (0, errors_1.err)('INVALID_PARAMS', `transitions 中的源状态 "${from}" 未在 states 中声明`);
        }
        if (!Array.isArray(targets)) {
            throw (0, errors_1.err)('INVALID_PARAMS', `transitions[${from}] 必须为数组`);
        }
        for (const to of targets) {
            if (!states.has(to)) {
                throw (0, errors_1.err)('INVALID_PARAMS', `transitions[${from}] 中的目标 "${to}" 未在 states 中声明`);
            }
        }
    }
}
exports.validateConfig = validateConfig;
/**
 * 创建状态机实例
 * @param config
 * @returns 状态机实例
 */
function createStateMachine(config) {
    validateConfig(config);
    const { initial, states, transitions, metadata = {} } = config;
    const stateSet = new Set(states);
    const transMap = new Map(Object.entries(transitions));
    function isValidState(s) {
        return stateSet.has(s);
    }
    function canTransition(from, to) {
        if (!isValidState(from) || !isValidState(to)) {
            return false;
        }
        const allowed = transMap.get(from) || [];
        return allowed.includes(to);
    }
    function assertTransition(from, to) {
        if (!isValidState(from)) {
            throw new IllegalTransitionError(from, to, [], `未知源状态："${from}"`);
        }
        if (!isValidState(to)) {
            throw new IllegalTransitionError(from, to, transMap.get(from) || [], `未知目标状态："${to}"`);
        }
        const allowed = transMap.get(from) || [];
        if (!allowed.includes(to)) {
            throw new IllegalTransitionError(from, to, allowed);
        }
    }
    function nextStates(from) {
        if (!isValidState(from)) {
            return [];
        }
        return [...(transMap.get(from) || [])];
    }
    function isTerminal(state) {
        if (!isValidState(state)) {
            return false;
        }
        return (transMap.get(state) || []).length === 0;
    }
    function getMetadata(state) {
        return metadata[state] || null;
    }
    return {
        initial,
        states: [...states],
        transitions: { ...transitions },
        isValidState,
        canTransition,
        assertTransition,
        nextStates,
        isTerminal,
        getMetadata,
        getAllowedTransitions: (from) => nextStates(from),
    };
}
exports.createStateMachine = createStateMachine;
/**
 * 高阶：根据状态 + 事件 + 守卫函数生成下一个状态
 *
 * @param sm - 状态机实例
 * @param from - 当前状态
 * @param event - 事件名（对应 transitions 表中的一项）
 * @param context - 守卫函数上下文
 * @returns 下一个状态，若不匹配返回 null
 *
 * @example
 *   const sm = createStateMachine({...})
 *   const next = applyEvent(sm, 'pending', 'confirm', { isAdmin: true })
 *   // 'confirmed' 或 'cancelled'（根据 context）
 */
function applyEvent(sm, from, event, context = {}) {
    if (!sm) {
        return null;
    }
    const allowed = sm.nextStates(from);
    return allowed.includes(event) ? event : null;
}
exports.applyEvent = applyEvent;
