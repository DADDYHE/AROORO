# 金面文字令牌化 · P1（必改：明暗两态都不达标）
#
# ✅ 已落地（2026-08-04 B2′ 直接应用，本文件仅作变更记录，勿再 apply）
#
# 病根（与裁定 1「状态色族缺了一格」同型）：
#   令牌层有 --text-on-primary（绿面反白 #F7F5EF）、有 --text-gold（浅底金字 #1F3A1F），
#   唯独没有「金面上的文字」这一格。于是 14 个消费者各自就近抓了个名字像的令牌，
#   出现 5 种写法压同一个面：
#     var(--text-inverse) ×5 · var(--text-on-primary) ×2 · var(--primary-deep) ×4
#     var(--card-color) ×1 · 硬编码 #FFFFFF ×2
#   前 4 类名字都在说"这是压在别的面上的字"，压到金面上全部失配。
#
# 事实（金族四个色停，相对亮度 L）：
#   #E8D5A8 L=0.6757 · #C9A24B L=0.3877 · #B8923E L=0.3110 · #A8894A L=0.2712
#   全部 > 纯白 4.5:1 所要求的上限 L ≤ 0.1833 → 金面上任何反白都不可能达标。
#   实测：纯白最低 1.45:1（partner .btn-accent），奶油最低 1.33:1。
#
# 解：新增 --text-on-accent: #1A1A17（已落 styles/theme-teal.wxss:57，本 patch 不含）
#   炭墨在四个色停上 = 12.05 / 7.27 / 6.00 / 5.27，最低 5.27 ✓
#   （深绿 #1F3A1F 最低 3.77 ✗、纯白 1.45 ✗、奶油 1.33 ✗ —— 炭墨是唯一全通解）
#
# P1 覆盖 10 处：明暗两态都不达标，属净修复，无取舍。
#   partner/common.wxss        .btn-accent / .action-btn-accent      1.45 → 5.27
#   mall/cart.wxss             .empty-btn / .checkout-btn            2.20 → 5.27
#   mall/order-confirm.wxss    .submit-btn / .retry-btn              2.40 → 5.27
#   mall/product-detail.wxss   .buy-btn / .sku-confirm-btn / .retry-btn  2.40 → 5.27
#   profile/mall-order-detail  .btn-primary                          2.40 → 5.27
#
# 渲染影响：金色按钮文字由白/奶油变炭墨。这是 B′ 金规范的既定方向
#   （--text-gold 已经把"浅底金字"改判成深绿，同一条逻辑的另一半）。
#   原"跨我的边界 8 处（mall/profile），需 team-lead 派单，故做 patch 不 apply"——
#   已于 2026-08-04 B2′ 由 prototype-builder-2 直接应用，本 P1 10 处全部落地为 var(--text-on-accent)。
#
# 应用：git apply -p1 scripts/patches/gold-ink-text-p1.patch
#   ⚠ 本 patch 已应用（2026-08-04），勿再 apply（会因变更已存在而失败）。
#   审计可用：git apply --reverse --check -p1 scripts/patches/gold-ink-text-p1.patch
# 回滚：git apply -R -p1 scripts/patches/gold-ink-text-p1.patch
#
diff -u -r a/subpackages/mall/cart.wxss b/subpackages/mall/cart.wxss
--- a/subpackages/mall/cart.wxss
+++ b/subpackages/mall/cart.wxss
@@ -290,7 +290,7 @@
 .empty-btn {
   padding: 18rpx 64rpx;
   background: var(--gradient-secondary);
-  color: var(--text-on-primary);
+  color: var(--text-on-accent);  /* 原 var(--text-on-primary) */
   border-radius: var(--border-radius-full);
   font-size: 26rpx;
   margin-top: var(--spacing-sm);
@@ -369,7 +369,7 @@
 .checkout-btn {
   padding: 20rpx 48rpx;
   background: var(--gradient-secondary);
-  color: var(--text-on-primary);
+  color: var(--text-on-accent);  /* 原 var(--text-on-primary) */
   border-radius: var(--border-radius-full);
   font-size: 28rpx;
   font-weight: 500;
diff -u -r a/subpackages/mall/order-confirm.wxss b/subpackages/mall/order-confirm.wxss
--- a/subpackages/mall/order-confirm.wxss
+++ b/subpackages/mall/order-confirm.wxss
@@ -559,7 +559,7 @@
   text-align: center;
   font-size: 28rpx;
   font-weight: 500;
-  color: var(--text-inverse);
+  color: var(--text-on-accent);  /* 原 var(--text-inverse) */
   letter-spacing: 0.10em;
   box-shadow: var(--shadow-glow-mall);
   transition: transform 280ms cubic-bezier(0.25,0.1,0.25,1),
@@ -616,7 +616,7 @@
   border-radius: var(--border-radius-full);
   font-size: 24rpx;
   font-weight: 500;
-  color: var(--text-inverse);
+  color: var(--text-on-accent);  /* 原 var(--text-inverse) */
   letter-spacing: 0.12em;
   box-shadow: var(--shadow-glow-mall);
   transition-property: transform;
diff -u -r a/subpackages/mall/product-detail.wxss b/subpackages/mall/product-detail.wxss
--- a/subpackages/mall/product-detail.wxss
+++ b/subpackages/mall/product-detail.wxss
@@ -418,7 +418,7 @@
   text-align: center;
   font-size: 26rpx;
   font-weight: 500;
-  color: var(--text-inverse);
+  color: var(--text-on-accent);  /* 原 var(--text-inverse) */
   letter-spacing: 0.15em;
   box-shadow: var(--shadow-glow-mall);
   font-family: var(--font-sans);
@@ -737,7 +737,7 @@
   text-align: center;
   font-size: 28rpx;
   font-weight: 500;
-  color: var(--text-inverse);
+  color: var(--text-on-accent);  /* 原 var(--text-inverse) */
   letter-spacing: 0.20em;
   box-shadow: var(--shadow-glow-mall);
   font-family: var(--font-sans);
@@ -795,7 +795,7 @@
   border-radius: var(--border-radius-xs);
   font-size: 24rpx;
   font-weight: 500;
-  color: var(--text-inverse);
+  color: var(--text-on-accent);  /* 原 var(--text-inverse) */
   letter-spacing: 0.20em;
   box-shadow: var(--shadow-glow-mall);
   font-family: var(--font-sans);
diff -u -r a/subpackages/partner/common.wxss b/subpackages/partner/common.wxss
--- a/subpackages/partner/common.wxss
+++ b/subpackages/partner/common.wxss
@@ -304,7 +304,7 @@
   justify-content: center;
   padding: 20rpx 48rpx;
   background: var(--lux-gold-grad);
-  color: #FFFFFF;
+  color: var(--text-on-accent);  /* 原 #FFFFFF */
   border-radius: var(--lux-radius-pill);
   font-size: 26rpx;
   font-weight: 500;
@@ -793,7 +793,7 @@
 
 .action-btn-accent {
   background: var(--lux-gold-grad);
-  color: #FFFFFF;
+  color: var(--text-on-accent);  /* 原 #FFFFFF */
   box-shadow: var(--lux-shadow-gold);
 }
 
diff -u -r a/subpackages/profile/mall-order-detail/index.wxss b/subpackages/profile/mall-order-detail/index.wxss
--- a/subpackages/profile/mall-order-detail/index.wxss
+++ b/subpackages/profile/mall-order-detail/index.wxss
@@ -308,7 +308,7 @@
 .btn-primary {
   padding: 20rpx 48rpx;
   background: var(--mall-gradient, linear-gradient(135deg, #C9A24B 0%, #A8894A 100%));
-  color: var(--card-color);
+  color: var(--text-on-accent);  /* 原 var(--card-color) */
   font-size: 28rpx;
   font-weight: 400;
   border-radius: var(--border-radius-full);
