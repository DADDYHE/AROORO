@import '../../../styles/loading-animation.wxss';

@import '../../../styles/variables.wxss';

.page {
  background: var(--background-color);
  min-height: 100vh;
  padding-bottom: 140rpx;
}

.loading-dot:nth-child(2) {
  animation-delay: 0.2s;
}

.loading-dot:nth-child(3) {
  animation-delay: 0.4s;
}

.status-banner {
  padding: 48rpx 32rpx 40rpx;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.status-banner.status-pending,
.status-banner.status-pending_payment {
  background: linear-gradient(135deg, #FF9800 0%, #F57C00 100%);
}

.status-banner.status-paid {
  background: linear-gradient(135deg, var(--feeding-primary) 0%, #1976D2 100%);
}

.status-banner.status-confirmed,
.status-banner.status-in_progress,
.status-banner.status-ongoing {
  background: linear-gradient(135deg, var(--mall-primary) 0%, var(--mall-secondary) 100%);
}

.status-banner.status-completed {
  background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
}

.status-banner.status-cancelled {
  background: linear-gradient(135deg, #9E9E9E 0%, #757575 100%);
}

.status-text {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--card-color);
  letter-spacing: 2rpx;
}

.status-desc {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.4;
}

.section {
  margin: 16rpx 24rpx;
  background: var(--card-color);
  border-radius: 24rpx;
  padding: 28rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.section-title {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 20rpx;
  letter-spacing: 1rpx;
}

.host-row {
  display: flex;
  gap: 20rpx;
  align-items: center;
}

.host-avatar {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: var(--background-color);
}

.host-avatar-placeholder {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: linear-gradient(135deg, #E8DEF8, #D1C4E9);
}

.host-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.host-name {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.host-contact {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.host-phone {
  font-size: 24rpx;
  color: var(--gray-700);
}

.call-btn {
  font-size: 22rpx;
  color: var(--mall-primary);
  padding: 6rpx 20rpx;
  border: 1rpx solid rgba(156, 39, 176, 0.3);
  border-radius: 20rpx;
  background: rgba(156, 39, 176, 0.05);
}

.call-btn:active {
  background: rgba(156, 39, 176, 0.12);
}

.pet-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.pet-item {
  display: flex;
  gap: 16rpx;
  align-items: center;
}

.pet-avatar {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: var(--background-color);
}

.pet-avatar-placeholder {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: linear-gradient(135deg, #FFE0B2, #FFCC80);
}

.pet-info {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.pet-name {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.pet-breed {
  font-size: 22rpx;
  color: var(--gray-500);
}

.pet-summary {
  padding: 8rpx 0;
}

.pet-names {
  font-size: 28rpx;
  color: var(--text-primary);
}

.date-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.row-icon {
  width: 28rpx;
  height: 28rpx;
  flex-shrink: 0;
}

.date-text {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--text-primary);
  flex: 1;
}

.date-days {
  font-size: 24rpx;
  color: var(--mall-primary);
  font-weight: 600;
  background: rgba(156, 39, 176, 0.08);
  padding: 6rpx 16rpx;
  border-radius: 16rpx;
}

.info-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--divider-color);
}

.info-row:last-child {
  border-bottom: none;
}

.info-label {
  font-size: 26rpx;
  color: var(--text-secondary);
}

.info-value {
  font-size: 26rpx;
  color: var(--text-primary);
  font-weight: 500;
  max-width: 400rpx;
  text-align: right;
  word-break: break-all;
}

.info-value.discount {
  color: #4CAF50;
}

.info-value-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.copy-btn {
  font-size: 22rpx;
  color: var(--mall-primary);
  padding: 4rpx 16rpx;
  border: 1rpx solid rgba(156, 39, 176, 0.3);
  border-radius: 16rpx;
  background: rgba(156, 39, 176, 0.05);
}

.copy-btn:active {
  background: rgba(156, 39, 176, 0.12);
}

.total-row {
  margin-top: 8rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid var(--gray-200);
  border-bottom: none;
}

.total-value {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--mall-primary);
}

.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 16rpx;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: var(--card-color);
  box-shadow: 0 -2rpx 12rpx rgba(0, 0, 0, 0.06);
}

.btn-primary {
  padding: 18rpx 48rpx;
  background: linear-gradient(135deg, var(--mall-primary) 0%, var(--mall-secondary) 100%);
  color: var(--card-color);
  font-size: 28rpx;
  font-weight: 600;
  border-radius: 36rpx;
  letter-spacing: 1rpx;
  text-align: center;
}

.btn-primary:active {
  opacity: 0.9;
  transform: scale(0.98);
}

.btn-secondary {
  padding: 18rpx 48rpx;
  background: var(--card-color);
  color: var(--gray-700);
  font-size: 28rpx;
  font-weight: 500;
  border-radius: 36rpx;
  border: 1rpx solid #D1D1D6;
  letter-spacing: 1rpx;
  text-align: center;
}

.btn-secondary:active {
  background: var(--background-color);
}
