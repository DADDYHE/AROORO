.gallery-page {
  min-height: 100vh;
  background: #F5F5F7;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 48rpx;
  box-sizing: border-box;
}

.gallery-space {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0;
}

.brand-exhibit {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 80rpx;
  animation: fadeUp 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.1s both;
}

.brand-cn {
  font-size: 56rpx;
  font-weight: 300;
  color: #1D1D1F;
  letter-spacing: 0.15em;
  line-height: 1.2;
}

.brand-en {
  font-size: 20rpx;
  font-weight: 400;
  color: rgba(59, 191, 181, 0.5);
  letter-spacing: 0.4em;
  margin-top: 12rpx;
  line-height: 1;
}

.brand-line {
  width: 80rpx;
  height: 3rpx;
  background: linear-gradient(90deg, #3BBFB5 0%, #35A99C 100%);
  margin-top: 32rpx;
  border-radius: 2rpx;
  animation: lineExpand 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.5s both;
}

.exhibit-card {
  width: 100%;
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 56rpx 48rpx 48rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.04);
  border: 1rpx solid rgba(134, 134, 139, 0.08);
  position: relative;
  overflow: hidden;
  animation: fadeUp 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.3s both;
}

.exhibit-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: linear-gradient(90deg, #3BBFB5 0%, #25A89B 50%, transparent 100%);
}

.exhibit-label {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 48rpx;
  align-self: flex-start;
}

.label-text {
  font-size: 22rpx;
  font-weight: 400;
  color: rgba(59, 191, 181, 0.6);
  letter-spacing: 0.1em;
}

.label-dot {
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background: #3BBFB5;
  opacity: 0.4;
}

.form-avatar-row {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 48rpx;
}

.avatar-frame {
  position: relative;
  width: 160rpx;
  height: 160rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 200rpx;
  height: 200rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(59, 191, 181, 0.12) 0%, rgba(59, 191, 181, 0) 70%);
  animation: breathe 3s ease-in-out infinite;
}

.avatar-picker-wrap {
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
  overflow: hidden;
  border: 2rpx solid rgba(59, 191, 181, 0.25);
  position: relative;
  z-index: 2;
  background: #FAFAFA;
}

.avatar-picker-btn-transparent {
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 160rpx;
  height: 200rpx;
  opacity: 0;
  padding: 0;
  margin: 0;
  z-index: 3;
}

.avatar-picker-btn-transparent::after {
  border: none;
}

.avatar-preview {
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
}

.avatar-placeholder {
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #FAFAFA;
  box-sizing: border-box;
}

.avatar-placeholder-icon {
  font-size: 48rpx;
  color: #C7C7CC;
  line-height: 1;
  font-weight: 200;
}

.avatar-caption {
  font-size: 20rpx;
  color: rgba(59, 191, 181, 0.5);
  letter-spacing: 0.08em;
  margin-top: 20rpx;
}

.form-nickname-row {
  width: 100%;
  animation: fadeUp 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.4s both;
}

.nickname-label {
  font-size: 20rpx;
  font-weight: 400;
  color: rgba(59, 191, 181, 0.5);
  letter-spacing: 0.08em;
  margin-bottom: 16rpx;
  display: block;
}

.nickname-input-wrap {
  position: relative;
  width: 100%;
}

.nickname-input {
  width: 100%;
  height: 80rpx;
  background: transparent;
  padding: 0 0 16rpx 0;
  font-size: 30rpx;
  color: #1D1D1F;
  font-weight: 400;
  letter-spacing: 0.02em;
  box-sizing: border-box;
}

.nickname-line {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 1rpx;
  background: rgba(59, 191, 181, 0.2);
}

.nickname-line::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 2rpx;
  background: linear-gradient(90deg, #3BBFB5 0%, #35A99C 100%);
  transition: width 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.nickname-input:focus + .nickname-line::after {
  width: 100%;
}

.action-exhibit {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 64rpx;
  animation: fadeUp 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.5s both;
}

.confirm-btn {
  width: 320rpx;
  height: 88rpx;
  line-height: 88rpx;
  background: linear-gradient(135deg, #3BBFB5 0%, #35A99C 100%);
  color: #FFFFFF;
  font-size: 30rpx;
  font-weight: 500;
  letter-spacing: 0.15em;
  border-radius: 48rpx;
  border: none;
  padding: 0;
  box-shadow: 0 8rpx 24rpx rgba(59, 191, 181, 0.25);
  position: relative;
  overflow: hidden;
}

.confirm-btn::after {
  border: none;
}

.confirm-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s ease;
}

.confirm-btn:active {
  transform: scale(0.96);
}

.confirm-btn.disabled {
  background: rgba(59, 191, 181, 0.25);
  color: rgba(255, 255, 255, 0.6);
  box-shadow: none;
}

.cancel-link {
  margin-top: 32rpx;
  padding: 16rpx 0;
}

.cancel-text {
  font-size: 24rpx;
  color: #AEAEB2;
  letter-spacing: 0.05em;
}

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(24rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes lineExpand {
  from {
    width: 0;
    opacity: 0;
  }
  to {
    width: 80rpx;
    opacity: 1;
  }
}

@keyframes breathe {
  0%, 100% {
    transform: translate(-50%, -50%) scale(1);
    opacity: 1;
  }
  50% {
    transform: translate(-50%, -50%) scale(1.15);
    opacity: 0.6;
  }
}
