@import '../../styles/loading-animation.wxss';

page {
  background: var(--background-color);
}

.page-container {
  min-height: 100vh;
  background: var(--background-color);
  padding-bottom: 160rpx;
}

/* ── 图片画廊 ── */
.image-gallery {
  position: relative;
  width: 100%;
  height: 600rpx;
  background: var(--gray-200);
  animation: fadeUp 0.6s ease-out;
}

.gallery-swiper {
  width: 100%;
  height: 100%;
}

.gallery-image {
  width: 100%;
  height: 100%;
}

.gallery-indicator {
  position: absolute;
  bottom: 28rpx;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 10rpx;
}

.indicator-dot {
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.4);
  transition: all 0.3s;
}

.indicator-dot.active {
  background: var(--card-color);
  width: 24rpx;
  border-radius: 5rpx;
}

.gallery-badge {
  position: absolute;
  top: 28rpx;
  left: 28rpx;
  display: flex;
  gap: 10rpx;
}

.badge-tag {
  padding: 8rpx 20rpx;
  border-radius: 32rpx;
  font-size: 20rpx;
  font-weight: 600;
  letter-spacing: 1rpx;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  background: rgba(255, 255, 255, 0.95);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.08);
}

.badge-tag.on_sale {
  color: var(--mall-primary);
}

.badge-tag.featured {
  color: var(--tuan-primary);
}

.gallery-actions {
  position: absolute;
  top: 28rpx;
  right: 28rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.gallery-action-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  background: rgba(255, 255, 255, 0.95);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
}

.gallery-action-btn:active {
  transform: scale(0.96);
}

.gallery-action-icon {
  width: 36rpx;
  height: 36rpx;
}

/* ── 页面内容区 ── */
.page-content {
  padding: 24rpx 32rpx;
}

/* ── 通用卡片 ── */
.info-card {
  background: var(--card-color);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.04);
  border: 1rpx solid rgba(134, 134, 139, 0.1);
  position: relative;
  overflow: hidden;
}

.info-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: linear-gradient(90deg, var(--mall-primary) 0%, var(--mall-secondary) 50%, transparent 100%);
  border-radius: 24rpx 24rpx 0 0;
}

/* ── 商品信息 ── */
.product-name {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text-primary);
  display: block;
  line-height: 1.3;
  margin-bottom: 8rpx;
}

.product-subtitle {
  font-size: 26rpx;
  color: var(--text-secondary);
  display: block;
  margin-bottom: 24rpx;
}

.price-section {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.current-price {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--mall-primary);
}

.original-price {
  font-size: 24rpx;
  color: var(--text-tertiary);
  text-decoration: line-through;
}

.discount-tag {
  font-size: 20rpx;
  font-weight: 600;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: rgba(156, 39, 176, 0.1);
  color: var(--mall-primary);
}

.meta-row {
  display: flex;
  gap: 32rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid var(--divider-color);
}

.meta-item {
  font-size: 22rpx;
  color: var(--text-secondary);
  letter-spacing: 0.5rpx;
}

.meta-item .value {
  color: var(--text-primary);
  font-weight: 600;
}

/* ── 区块标题 ── */
.section-label {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  display: block;
  margin-bottom: 24rpx;
  letter-spacing: 1rpx;
}

/* ── 商品详情 ── */
.description-text {
  font-size: 28rpx;
  color: var(--text-primary);
  line-height: 1.8;
  margin-bottom: 28rpx;
}

.detail-images {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.detail-image {
  width: 100%;
  border-radius: 16rpx;
}

/* ── 底部操作栏 ── */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-top: 1rpx solid rgba(134, 134, 139, 0.1);
  display: flex;
  align-items: center;
  gap: 24rpx;
  z-index: 100;
}

.bar-actions {
  display: flex;
  gap: 24rpx;
}

.bar-action-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  position: relative;
}

.contact-btn {
  display: flex !important;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  position: relative;
  padding: 0 !important;
  margin: 0 !important;
  background: transparent !important;
  border: none !important;
  line-height: normal !important;
  min-width: 0 !important;
  width: auto !important;
  font-size: inherit !important;
  box-sizing: border-box;
}

.contact-btn::after {
  border: none !important;
}

.bar-action-icon-img {
  width: 40rpx;
  height: 40rpx;
}

.bar-action-text {
  font-size: 18rpx;
  color: var(--gray-500);
  letter-spacing: 1rpx;
}

.cart-badge {
  position: absolute;
  top: -8rpx;
  right: -16rpx;
  min-width: 28rpx;
  height: 28rpx;
  line-height: 28rpx;
  text-align: center;
  background: var(--error-color);
  color: #fff;
  font-size: 18rpx;
  border-radius: 14rpx;
  padding: 0 6rpx;
}

.buy-buttons {
  flex: 1;
  display: flex;
  gap: 16rpx;
}

.cart-btn {
  flex: 1;
  padding: 22rpx 0;
  border: 2rpx solid rgba(156, 39, 176, 0.3);
  border-radius: 48rpx;
  text-align: center;
  font-size: 26rpx;
  font-weight: 600;
  color: var(--mall-primary);
  letter-spacing: 2rpx;
  background: linear-gradient(135deg, var(--card-color) 0%, #FAF5FC 100%);
  transition: all 0.3s ease;
}

.cart-btn:active {
  transform: scale(0.98);
  background: rgba(156, 39, 176, 0.08);
}

.buy-btn {
  flex: 1;
  padding: 22rpx 0;
  background: linear-gradient(135deg, var(--mall-primary) 0%, var(--mall-secondary) 100%);
  border-radius: 48rpx;
  text-align: center;
  font-size: 26rpx;
  font-weight: 600;
  color: var(--card-color);
  letter-spacing: 2rpx;
  box-shadow: 0 6rpx 16rpx rgba(156, 39, 176, 0.3);
  transition: all 0.3s ease;
}

.buy-btn:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 12rpx rgba(156, 39, 176, 0.3);
}

/* ── 规格选择弹窗 ── */
.sku-popup-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 200;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.sku-popup-mask.show {
  opacity: 1;
  visibility: visible;
}

.sku-popup {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 201;
  background: var(--card-color);
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  max-height: 75vh;
  display: flex;
  flex-direction: column;
}

.sku-popup.show {
  transform: translateY(0);
}

.sku-popup-header {
  display: flex;
  padding: 32rpx;
  border-bottom: 1rpx solid var(--divider-color);
  position: relative;
  flex-shrink: 0;
}

.sku-popup-thumb {
  width: 160rpx;
  height: 160rpx;
  border-radius: 16rpx;
  background: var(--divider-color);
  flex-shrink: 0;
}

.sku-popup-info {
  flex: 1;
  margin-left: 24rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.sku-popup-price {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--mall-primary);
  margin-bottom: 8rpx;
}

.sku-popup-stock {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-bottom: 4rpx;
}

.sku-popup-selected {
  font-size: 24rpx;
  color: var(--text-primary);
}

.sku-popup-close {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: var(--text-tertiary);
}

.sku-popup-body {
  flex: 1;
  overflow-y: auto;
  min-width: 0;
  -webkit-overflow-scrolling: touch;
}

.sku-popup-body-inner {
  padding: 24rpx 32rpx;
}

.sku-group {
  margin-bottom: 32rpx;
}

.sku-group-label {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  display: block;
  margin-bottom: 20rpx;
}

.sku-options {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.sku-option {
  padding: 16rpx 32rpx;
  border-radius: 40rpx;
  background: var(--divider-color);
  border: 2rpx solid transparent;
  transition: all 0.2s ease;
}

.sku-option.selected {
  background: rgba(156, 39, 176, 0.08);
  border-color: var(--mall-primary);
}

.sku-option.disabled {
  opacity: 0.4;
  pointer-events: none;
}

.sku-option-name {
  font-size: 26rpx;
  color: var(--text-primary);
  font-weight: 500;
}

.sku-option.selected .sku-option-name {
  color: var(--mall-primary);
  font-weight: 600;
}

.sku-quantity {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--divider-color);
  overflow: hidden;
}

.sku-quantity .sku-group-label {
  margin-bottom: 0;
  flex-shrink: 0;
}

.quantity-control {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-shrink: 0;
}

.quantity-btn {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: var(--divider-color);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: var(--text-primary);
  font-weight: 500;
  transition: all 0.2s ease;
}

.quantity-btn.disabled {
  opacity: 0.35;
}

.quantity-btn:active {
  background: var(--gray-200);
}

.quantity-value {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-primary);
  min-width: 48rpx;
  text-align: center;
}

.sku-popup-footer {
  padding: 24rpx 32rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--divider-color);
  flex-shrink: 0;
}

.sku-confirm-btn {
  width: 100%;
  padding: 24rpx 0;
  background: linear-gradient(135deg, var(--mall-primary) 0%, var(--mall-secondary) 100%);
  border-radius: 48rpx;
  text-align: center;
  font-size: 30rpx;
  font-weight: 600;
  color: var(--card-color);
  letter-spacing: 2rpx;
  box-shadow: 0 6rpx 16rpx rgba(156, 39, 176, 0.3);
}

.sku-confirm-btn:active {
  transform: scale(0.98);
}

.sku-confirm-btn.disabled {
  background: var(--text-tertiary);
  box-shadow: none;
  pointer-events: none;
}

/* ── 加载状态 ── */

.loading-dot:nth-child(2) {
  animation-delay: 0.2s;
}

.loading-dot:nth-child(3) {
  animation-delay: 0.4s;
}

/* ── 错误状态 ── */
.error-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: var(--background-color);
  padding: 0 40rpx;
}

.error-icon {
  font-size: 80rpx;
  margin-bottom: 24rpx;
  color: var(--text-tertiary);
}

.error-text {
  font-size: 28rpx;
  color: var(--text-secondary);
  text-align: center;
  margin-bottom: 40rpx;
}

.retry-btn {
  padding: 18rpx 56rpx;
  background: linear-gradient(135deg, var(--mall-primary) 0%, var(--mall-secondary) 100%);
  border-radius: 48rpx;
  font-size: 24rpx;
  font-weight: 600;
  color: var(--card-color);
  letter-spacing: 2rpx;
  box-shadow: 0 6rpx 16rpx rgba(156, 39, 176, 0.3);
}

.retry-btn:active {
  transform: scale(0.98);
}

/* ── 动画 ── */

.card-animate {
  animation: fadeUp 0.5s ease-out both;
}

.card-animate-1 { animation-delay: 0.1s; }
.card-animate-2 { animation-delay: 0.2s; }
.card-animate-3 { animation-delay: 0.3s; }
