--- a/custom-tab-bar/index.wxss
+++ b/custom-tab-bar/index.wxss
@@ -8,8 +8,9 @@
      · 图标线条极细，文字极轻
    ----------------------------------------------------------------
    设计系统：styles/theme-teal.wxss（深森林绿体系 · 事实生效层）
-   注意：styles/design-tokens.wxss 的 --zy-tabbar-* 为橄榄绿旧值，
-        已被 app.wxss 中后置的 theme-teal.wxss 覆盖，勿直接依赖。
+   令牌策略：本组件是全仓唯一 tabBar 实现（app.json tabBar.custom=true），
+        专用几何/材质值不进全局命名空间，就地声明为 --tb-*；
+        与调色板同值的颜色则直接消费调色板主令牌，随主题联动。
    ================================================================ */
 
 /* 显式导入令牌层：即使组件样式隔离，也保证令牌在本组件作用域可解析 */
@@ -55,8 +56,10 @@
      声明在 .tab-bar 而非 :host：本组件 WXML 用 <root-portal> 包裹，
      子树会被搬出宿主节点，:host 上的自定义属性无法继承进来。
      .tab-bar 是 portal 子树的根，其后代（中心按钮等）均可继承。
-     以下值在 theme-teal 中无精确等价物，就地令牌化以保证零外观改变。 */
-  --tb-bar-bg: rgba(247, 245, 239, 0.96);          /* 奶油米白半透底（非 --zy-tabbar-bg 的纯白） */
+     以下值在调色板中无精确等价物（多为 alpha 变体，WXSS 无法由 hex 令牌算出），
+     就地令牌化；与调色板同值的颜色不在此列，直接消费主令牌。 */
+  --tb-bar-bg: rgba(247, 245, 239, 0.96);          /* 奶油米白半透底 = --zy-bg 的 96% alpha */
+  --tb-bar-border: 1rpx solid rgba(26, 26, 23, 0.06);  /* 炭黑发丝线（--hairline 是 0.08，非同值） */
   --tb-center-shadow:
     0 6rpx 20rpx rgba(31, 58, 31, 0.28),
     0 2rpx 8rpx rgba(31, 58, 31, 0.18);            /* 深林绿投影（非橄榄绿） */
@@ -73,7 +76,7 @@
   right: 0;
   height: 104rpx;
   background: var(--tb-bar-bg);
-  border-top: var(--zy-tabbar-border-top);
+  border-top: var(--tb-bar-border);
   display: flex;
   justify-content: space-around;
   align-items: center;
@@ -189,7 +192,7 @@
 .tab-bar-text {
   font-size: 18rpx;
   font-weight: 400;
-  color: var(--zy-tabbar-item-inactive);
+  color: var(--text-tertiary);
   line-height: 1;
   transition: opacity 320ms cubic-bezier(0.19, 1, 0.22, 1),
               transform 320ms cubic-bezier(0.19, 1, 0.22, 1);
@@ -198,7 +201,7 @@
 }
 
 .tab-bar-item.active .tab-bar-text {
-  color: var(--zy-tabbar-item-active);
+  color: var(--primary-color);
   font-weight: 500;
   letter-spacing: 0.06em;
   opacity: 1;
@@ -226,7 +229,7 @@
   width: 104rpx;
   height: 104rpx;
   border-radius: 50%;
-  background: var(--zy-tabbar-center-grad);
+  background: var(--gradient-primary);
   position: relative;
   overflow: visible;
   box-shadow: var(--tb-center-shadow);
